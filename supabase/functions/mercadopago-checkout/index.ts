import{adminClient,authenticatedUser,corsHeaders,credential,externalReference,json,mpFetch}from'./mercadopago-shared.ts';
const functionBase=`${Deno.env.get('SUPABASE_URL')}/functions/v1`;
Deno.serve(async(req)=>{if(req.method==='OPTIONS')return new Response('ok',{headers:corsHeaders});const db=adminClient();try{const user=await authenticatedUser(req,db),body=await req.json(),comercioId=String(body.comercioId||''),pedidoId=String(body.pedidoId||'');const{data:pedido}=await db.from('pedidos_online').select('*,pedido_online_items(*)').eq('id',pedidoId).eq('comercio_id',comercioId).eq('cliente_user_id',user.id).single();if(!pedido)throw new Error('Pedido no encontrado');if(pedido.estado_pago==='aprobado')throw new Error('El pedido ya esta pagado');const{data:config}=await db.from('mercadopago_configuraciones').select('*').eq('comercio_id',comercioId).eq('connected',true).eq('checkout_habilitado',true).single();if(!config)throw new Error('Mercado Pago no esta habilitado para este comercio');const cred=await credential(db,comercioId),ref=externalReference(comercioId,'pedido',pedido.id),idempotencyKey=crypto.randomUUID();const{data:op,error:opError}=await db.from('mercadopago_operaciones').insert({comercio_id:comercioId,pedido_online_id:pedido.id,origen:'tienda_online',modalidad:'checkout_pro',ambiente:config.ambiente,external_reference:ref,idempotency_key:idempotencyKey,importe:Number(pedido.total),moneda:'ARS'}).select().single();if(opError)throw opError;const origin=String(body.returnOrigin||'').replace(/\/$/,'');const allowedOrigins=(Deno.env.get('MP_ALLOWED_STORE_ORIGINS')||'').split(',').map(v=>v.trim()).filter(Boolean);if((!origin.startsWith('https://')&&!origin.startsWith('http://localhost'))||(allowedOrigins.length&&!allowedOrigins.includes(origin)))throw new Error('Origen de retorno no autorizado');const payload={items:pedido.pedido_online_items.map((i:any)=>({id:i.producto_id,title:i.descripcion,quantity:i.cantidad,unit_price:Number(i.precio_unitario),currency_id:'ARS'})),payer:{email:pedido.cliente_email,name:pedido.cliente_nombre},external_reference:ref,back_urls:{success:`${origin}/pago/aprobado?pedido=${pedido.id}`,pending:`${origin}/pago/pendiente?pedido=${pedido.id}`,failure:`${origin}/pago/rechazado?pedido=${pedido.id}`},auto_return:'approved',notification_url:`${functionBase}/mercadopago-webhook?op=${op.id}`,metadata:{operacion_id:op.id,comercio_id:comercioId,pedido_id:pedido.id}};try{const preference=await mpFetch('/checkout/preferences',cred.access_token,{method:'POST',headers:{'X-Idempotency-Key':idempotencyKey},body:JSON.stringify(payload)}),checkoutUrl=config.ambiente==='production'?preference.init_point:preference.sandbox_init_point;await db.from('mercadopago_operaciones').update({preference_id:preference.id,checkout_url:checkoutUrl,raw_response:preference}).eq('id',op.id);await db.from('pedidos_online').update({estado_pago:'pendiente'}).eq('id',pedido.id);return json({checkoutUrl,operacionId:op.id});}catch(error){await db.from('mercadopago_operaciones').update({estado:'error',estado_detalle:String(error)}).eq('id',op.id);throw error;}}catch(error){return json({error:error instanceof Error?error.message:String(error)},400);}});
